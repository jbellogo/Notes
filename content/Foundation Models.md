They signify a paradigm shift in AI 

Before, you trained one model per data set to achieve one specific function (narrow AI). 

A team from stanford predicted we would get to a point of having singular foundation models that can drive the same use cases and applications that several trained models used to independently. and the same model can be transfered to any number of tasks 

what gives this model this superpoweer to transfer to multiple diferent tasks is that it is trained: on a **huge amount** in an **unsupervised manner** on **unstructured data**. 
Training data: 
* Huge amount: Gigabytes/petabytes
* Unsupervised:
* Unstructured